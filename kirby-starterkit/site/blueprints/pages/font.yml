# Each page blueprint must have a title, the title may be different from the file name
title: Fonte
# Sorting number scheme that is applied when page status is changed to `listed`
# More about page sorting: https://getkirby.com/docs/reference/panel/blueprints/page#sorting
num: date
# Each page can have an icon that is shown in page listings when no preview image is available.
icon: 📖
# Custom descriptions for each page status depending on use case
# More about page status: https://getkirby.com/docs/reference/panel/blueprints/page#statuses
status:
  draft:
    label: Draft
    text: The font is still in draft mode. It can only be seen by editors with panel access.
  unlisted:
    label: In Review
    text: The font is online and can be visited with the direct URL. The team must still give the final go to publish it.
  listed:
    label: Published
    text: The font is online and listed in the blog
# Define the form layout with two columns
# Columns are optional but help structure the form layout
# More about columns: https://getkirby.com/docs/guide/blueprints/layout#defining-columns
columns:
  - width: 2/3
    # This columns only has a single field
    fields:
      tags:
        label: Tags
        type: tags
        options:
          - value: category/display
            text: display
          - value: category/serif
            text: serif
          - value: category/sansserif
            text: sans serif
          - value: category/mono
            text: mono

          - value: category/1style
            text: 1 style
          - value: category/2styles
            text: 2 styles
          - value: category/3stylesetplus
            text: 3 styles et +                
          - value: category/variable
            text: variable

          - value: category/minuscules
            text: minuscules
          - value: category/majuscules
            text: majuscules
          - value: category/chiffres
            text: chiffres               
          - value: category/chspeciaux
            text: charactères spéciaux
          - value: category/inclusif
            text: glyphes inclusifs (QUNI)

      description:
        type: textarea
        autofocus: true
        label: Description de la fonte
      
      #fichier de fonte
        files:
          type: files

      #fonte variable ou non
      toggle:
        label: fonte variable
        type: toggle
        required: true
        width: 1/5

      nom de l'axe variable 1:
        type: text
        width: 1/4
        when:
          toggle: true
      number:
        label: Valeur minimum axe variable 1
        type: number
        placeholder: 0
        width: 1/4
        when:
          toggle: true
      number2:
        label: Valeur maximum axe variable 1
        type: number
        placeholder: 1000
        width: 1/4
        when:
          toggle: true
        

#si oui -> axe 1: nom de l'axe, valeur min, valeur max
#       -> axe 2: nom de l'axe, valeur min, valeur max
      category:
        label: Licence
        type: radio
        width: 1/5
        required: true
        options:
          ofl: OFL
          sil: SIL
          autre: autre
  - width: 1/3
    # This second column holds a fields section with multiple fields
    # More about fields sections: https://getkirby.com/docs/reference/panel/sections/fields
    sections:
      meta:
        type: fields
        fields:
          # If you need the same field in multiple locations, you can define them once and reuse whereever needed.
          # Here we use a files field defined in `/site/blueprints/field/cover.yml`
          # cover: fields/cover
          date:
            type: date
            display: YYYY
            time: true
            default: now

          # Fields types can be added using the shortcut `fieldname: true`
          # (however, you can only use a fieldname once per page and this only makes sense for simple fields without configuration)
          #images: up to 5 images. Description: Largeur minimum: 1200px
          downloads:
            type: files
            label: "Images format paysage, largeur minimum 1200 pixels. 5 images max"
            description: "Sélectionnez des images, PNG/JPG, largeur minimum 1200px. 5 images maximum"
            
            min: 0
            max: 5

          name:
            label: Prénom Nom
            type: text
            minlength: 10
            maxlength: 40
            required: true
              
          toggle2:
            label: Fonte téléchargeable directement depuis la typothèque
            required: true
            type: toggle
            text: 
              - "non"
              - "oui"

          email:
            label: adresse email
            type: text
            required: true


          
          insta:
            label: instagram
            type: url

          site:
            label: site personnel
            type: url
          
                    




